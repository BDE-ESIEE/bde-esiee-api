{
  "routes": [
    {
      "method": "GET",
      "path": "/companies",
      "handler": "companies.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/companies",
      "handler": "companies.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/companies",
      "handler": "companies.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
